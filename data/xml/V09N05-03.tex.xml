<?xml version="1.0" ?>
<root>
  <title>文字列を$k$回以上含む文書数の計数アルゴリズム</title>
  <abstract>この論文で計算するものは，ある文字列を$k$回以上含むドキュメントの総数(df_k)である．全ての部分文字列に対してこれらの統計量を保存する場合O(N^2)の表が必要となり，コーパスの大きさを考えると，この表は実用的でなく，通常の計算機では実際に作ることは難しい．しかし，k=1の場合，SuffixArray，文字列のクラス分けを利用して，統計量をクラス毎に保存することで，これをO(N)の表にできるという報告がある．このクラスは同じ統計量を持つ文字列の集合であり，コーパス内の全ての文字列の統計量はクラス毎に作成した統計量の表から取り出すことができる．しかし，この方法はk≧2の場合には使用できない．我々は，k≧2の場合にも使用でき，表を用いることによって文字列の統計量を計算するアルゴリズムを提案する．本稿ではdf_kの性質を述べた後，単純な計算方法と提案するアルゴリズムとの比較を行う．このアルゴリズムは，前処理として表を作成するためにO(NlogN)の計算時間とO(N)のメモリを使用し，その表を用いてO(logN)時間で文字列の統計量を取り出すことができる．</abstract>
  <keywords>文字列頻度，文書頻度，SuffixArray</keywords>
</root>
